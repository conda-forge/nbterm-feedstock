{% set name = "nbterm" %}
{% set version = "0.0.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: d77dab1449f10410a045acd93e977a53e1e7b10978d3044c9d7b906396ae7104

build:
  noarch: python
  number: 0
  entry_points:
    - nbterm = nbterm.nbterm:cli
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - python >=3.7
    - prompt-toolkit >=3.0.16
    - typer
    - pygments
    - rich
    - kernel_driver

test:
  commands:
    - nbterm --help

about:
  home: https://github.com/davidbrochart/nbterm
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: A tool for viewing, editing and executing Jupyter Notebooks in the terminal
  dev_url: https://github.com/davidbrochart/nbterm

extra:
  recipe-maintainers:
    - davidbrochart
